#include <stdio.h>

int main()
{   int n=5,i,j,k;
    for(i=0;i<n;i++)
    {    
       for(j=n;j>i;j--)
       {       
          printf(" ");
       }
       for(k=0;k<=i;k++)
       {
          printf("%c",k+65);
       }
    printf("\n");
}
    return 0;
}
